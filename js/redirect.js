// JS backup redirect in case meta refresh is blocked
window.addEventListener('DOMContentLoaded', () => {
  const target = 'https://discord.gg/GVMGZuGZ8F';
  setTimeout(() => { window.location.replace(target); }, 50);
});
